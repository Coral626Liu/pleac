@@@1.4

HAL -> IBM

Can't use Array#each since I can't change a Fixnum object "in place".

I use Array#each_index; I thought of using Array#collect (which gives a
block more similar to the spirit of the Perl example, but needs an
affectation...), but I'm not satisfied by any solution.
